version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No prebuild needed"
    build:
      commands:
        - echo "No build needed for static calculator"
  artifacts:
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
