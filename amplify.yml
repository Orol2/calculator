version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - output/**/*
      - .spago/**/*
